  <script type="text/x-handlebars" data-template-name="searches">
    <div class="container">
      <div class="row">
        <div class="span12">
          <section id="todoapp">
            <header id="header">
              <h1>Blacklight</h1>
              {{input type="text" id="q" placeholder="Search for..."
                value=query action="runSearch"}}
            </header>

            <section id="main">
              {{#each}}
              <li>{{id}} {{title_display}}</li>
              {{/each}}
            </section>
          </section>
        </div>
      </div>
    </div>
    </script>

